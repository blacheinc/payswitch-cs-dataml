# Deprecated: transformation-service is decoupled from schema-mapping-service.
# This script intentionally no-ops to avoid accidental bundle sync workflows.
Write-Host "sync_schema_mapping_bundle.ps1 is deprecated and does nothing." -ForegroundColor Yellow
Write-Host "Transformation-service now uses local anonymization modules only." -ForegroundColor Yellow
